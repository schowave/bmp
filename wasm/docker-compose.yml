services:
  bmp:
    image: schowave/bmp:wasm
    container_name: bmp-wasm
    restart: unless-stopped
    ports:
      - "8090:8080"
    volumes:
      - /volume1/docker/bmp-saves:/data
    labels:
      - "com.centurylinklabs.watchtower.enable=true"
